* resolve tbnz/tbz that are out of range [x]
* resolve labels like 1f, 1b [x]
* group basic blocks together and assign a range register
* make sure it works with the loader
* patch to LLD
* set up pre-built toolchain with instrumented musl and compiler-rt, and LLD
